package com.example.navdemo.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette ──────────────────────────────────────────────────────────
val NeonBlue         = Color(0xFF00D4FF)
val NeonBlueLight    = Color(0xFF80EAFF)
val NeonBlueDim      = Color(0xFF007090)
val NeonBlueGlow     = Color(0x3300D4FF)

// ── Background / surface ─────────────────────────────────────────────────────
val DarkBackground   = Color(0xFF040810)   // near-black with slight blue tint
val DarkSurface      = Color(0xFF0A1628)   // slightly lighter
val DarkCard         = Color(0xFF0D1E35)

// ── Glass overlay ─────────────────────────────────────────────────────────────
val GlassDark        = Color(0xCC040D1A)   // 80 % opaque dark navy
val GlassMid         = Color(0x99040D1A)   // 60 % opaque
val GlassLight       = Color(0x33FFFFFF)   // white tint for rim highlights
val GlassBorder      = Color(0x4400D4FF)   // neon-blue rim

// ── Accents ───────────────────────────────────────────────────────────────────
val AccentGreen      = Color(0xFF00FF8C)   // speed / status OK
val AccentAmber      = Color(0xFFFFB800)   // warning
val AccentRed        = Color(0xFFFF3A3A)   // alert

// ── Text ─────────────────────────────────────────────────────────────────────
val TextPrimary      = Color(0xFFE4F4FF)
val TextSecondary    = Color(0xFF7BAFD4)
val TextDimmed       = Color(0xFF3A6080)
